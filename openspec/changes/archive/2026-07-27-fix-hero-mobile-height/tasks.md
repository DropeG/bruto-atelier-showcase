## 1. HeroSection Height Fix

- [x] 1.1 Update `HeroSection.tsx` container class to use `h-[100svh]` for mobile viewport sizing while retaining `md:h-screen` for desktop.
- [x] 1.2 Verify that `<picture>` and `<img>` fill 100% height without leaving gaps at the bottom.

## 2. Verification & Testing

- [x] 2.1 Test in Mobile responsive mode and build sanity check.
- [x] 2.2 Verify layout on mobile browsers to confirm no bleeding of `ImageRow 1` photos.

